editors:
  - version: 2019.1.0b4
platforms:
  - name: win
    type: Unity::VM
    image: package-ci/win10:latest
    flavor: m1.large
projects:
  - name: ShaderGraph
  - name: VisualEffectGraph
  - name: LWGraphicsTest
  - name: HDRP_Tests
  - name: PostProcessing
  testplatforms:
  - name: StandaloneWindows64
  - name: StandaloneOSXIntel64
  - name: playmode
  - name: editmode
---
{% for editor in editors %}
{% for platform in platforms %}
{% for project in projects %}
{{ project.name }}_{{ platform.name }}_{{ editor.version }}:
  name : Test {{ project.name }} on version {{ editor.version }} on {{ platform.name }}
  agent:
    type: {{ platform.type }}
    image: {{ platform.image }}
    flavor: {{ platform.flavor}}
  commands:
      - npm install upm-ci-utils -g --registry https://api.bintray.com/npm/unity/unity-npm
      - upm-ci project test -u {{ editor.version }} --project-path ./TestProjects/{{ project.name }}/
  triggers:
    branches:
      only:
        - yamato-test
  artifacts:
    logs.zip:
      paths:
        - "upm-ci~/test-results/**/*"
        
{% endfor %}
{% endfor %}
{% endfor %}